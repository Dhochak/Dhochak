pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                retry(3) {
                    sh './test.sh'
                }

                timeout(time: 5, unit: 'MINUTES') {
                    sh './test_bkp.sh'
                }
            }
        }
    }
}
