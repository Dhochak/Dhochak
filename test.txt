pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                retry(3) {
                    sh './test.sh'
                }

                timeout(time: 1, unit: 'MINUTES') {
                    sh './test_bkp.sh'
                }
            }
        }
    }
}
