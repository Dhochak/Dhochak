pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh './test.sh'
            }
        }
    }
    post {
        always {
            junit 'build/reports/**/*.xml'
        }
    }
}
